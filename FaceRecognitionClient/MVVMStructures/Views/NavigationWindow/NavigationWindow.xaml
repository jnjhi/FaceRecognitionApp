<Window x:Class="FaceRecognitionClient.Views.NavigationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Navigation" Height="800" Width="1200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.ColumnSpan="3" Content="Logout"
                Command="{Binding LogOutCommand}"
                HorizontalAlignment="Right" Margin="10"
                Width="100" Style="{StaticResource ModernButton}"/>
        <Button Grid.Row="1" Grid.Column="0" Content="Face Recognition"
                Command="{Binding OpenFaceRecognitionCommand}"
                Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center"
                Style="{StaticResource ModernButton}"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Gallery"
                Command="{Binding OpenGalleryCommand}"
                Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center"
                Style="{StaticResource ModernButton}"/>
        <Button Grid.Row="1" Grid.Column="2" Content="Attendance"
                Command="{Binding OpenAttendanceCommand}"
                Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center"
                Style="{StaticResource ModernButton}"/>
    </Grid>
</Window>
