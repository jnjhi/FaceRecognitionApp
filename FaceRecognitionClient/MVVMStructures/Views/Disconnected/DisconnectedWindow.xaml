<Window x:Class="FaceRecognitionClient.Views.DisconnectedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Disconnected" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">
    <Border Grid.Column="1" Width="400"  Height="300" Padding="40" Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1" VerticalAlignment="Center">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
             <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,20" TextAlignment="Center"/>
             <Button Content="Log In" Command="{Binding LogInAgainCommand}" Width="120" Height="40" Background="#6A5ACD" Foreground="White" Style="{StaticResource ModernButton}"/>
        </StackPanel>
    </Border>
</Window>
